{
  "name": "navbar-dropdown-v1",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.cav-lab.io/",
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^4.11.0",
    "react-router-dom": "^5.3.4",
    "react-scroll": "^1.8.9"
  },
  "scripts": {
    "start": "vite --host 0.0.0.0",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "postbuild": "node scripts/create-route-pages.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && gh-pages -d build && npx --yes wrangler deploy",
    "deploy:github": "npm run build && gh-pages -d build",
    "deploy:cloudflare": "npm run build && npx --yes wrangler deploy"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "gh-pages": "^4.0.0",
    "vite": "^4.5.14"
  }
}
