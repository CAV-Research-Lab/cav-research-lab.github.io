# Archive

Files in this folder are retained for reference only. They are not imported by the live app.

Move code out of `archive/` before reusing it, then update its imports to match the active source structure.
